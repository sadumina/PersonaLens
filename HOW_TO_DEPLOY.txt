╔════════════════════════════════════════════════════════════════════════════╗
║                                                                            ║
║                    PersonaLens - DEPLOYMENT GUIDE                          ║
║                    How to Deploy Your Application                          ║
║                                                                            ║
╚════════════════════════════════════════════════════════════════════════════╝


📋 CHOOSE YOUR DEPLOYMENT METHOD:

┌────────────────────────────────────────────────────────────────────────────┐
│  OPTION 1: Quick Deploy with Docker (RECOMMENDED - Easiest)               │
└────────────────────────────────────────────────────────────────────────────┘

⏱️  Time: 10 minutes
💰 Cost: Free (local) or $6-12/month (VPS)
🎯 Best for: Quick start, testing, small production

STEPS:

1️⃣  Get a server (choose one):
   • Your local computer (for testing)
   • DigitalOcean Droplet ($6-12/month) ✅ RECOMMENDED
   • AWS EC2 instance
   • Google Cloud VM
   • Azure VM

2️⃣  Install Docker (one-time setup):
   
   curl -fsSL https://get.docker.com -o get-docker.sh
   sh get-docker.sh
   sudo apt install docker-compose -y

3️⃣  Clone and configure:
   
   git clone https://github.com/sadumina/PersonaLens.git
   cd PersonaLens
   cp personalens-backend/.env.example personalens-backend/.env
   nano personalens-backend/.env

4️⃣  Edit the .env file:
   
   MONGODB_URI=your-mongodb-atlas-uri
   JWT_SECRET=your-32-character-secret-key
   CORS_ORIGINS=["http://your-domain.com"]

5️⃣  Deploy (single command):
   
   docker-compose up --build -d

6️⃣  Access your app:
   
   Frontend: http://your-server-ip
   Backend:  http://your-server-ip:8000
   API Docs: http://your-server-ip:8000/docs

✅ DONE! Your app is live!


┌────────────────────────────────────────────────────────────────────────────┐
│  OPTION 2: Cloud Platform Deploy (One-Click-ish)                          │
└────────────────────────────────────────────────────────────────────────────┘

⏱️  Time: 15-30 minutes
💰 Cost: Varies by platform
🎯 Best for: Production, scalability

A. DigitalOcean (Simplest Cloud):
   
   1. Create Account → New Droplet
   2. Choose: Ubuntu 22.04, 2GB RAM ($12/month)
   3. SSH into droplet
   4. Follow "Option 1" steps above
   5. Point domain to droplet IP
   6. Setup SSL: sudo certbot --nginx -d yourdomain.com

B. Heroku (No server management):
   
   heroku create personalens-backend
   heroku create personalens-frontend
   heroku config:set MONGODB_URI=your-uri JWT_SECRET=your-secret
   heroku container:push web -a personalens-backend
   heroku container:release web -a personalens-backend

C. AWS EC2:
   
   1. Launch EC2 instance (t3.small)
   2. Security Group: Allow ports 22, 80, 443
   3. SSH and follow "Option 1" steps
   4. Setup domain and SSL

D. Google Cloud Run (Serverless):
   
   gcloud builds submit --tag gcr.io/PROJECT_ID/personalens-backend
   gcloud run deploy personalens-backend --image gcr.io/PROJECT_ID/personalens-backend


┌────────────────────────────────────────────────────────────────────────────┐
│  OPTION 3: Manual Deployment (Advanced)                                   │
└────────────────────────────────────────────────────────────────────────────┘

⏱️  Time: 1-2 hours
💰 Cost: Varies
🎯 Best for: Custom setups, learning

Backend:
   cd personalens-backend
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   cp .env.example .env
   # Edit .env
   uvicorn app.main:app --host 0.0.0.0 --port 8000

Frontend:
   cd personalens-frontend
   npm install
   cp .env.example .env
   # Edit .env with backend URL
   npm run build
   # Serve dist/ with nginx or Apache


╔════════════════════════════════════════════════════════════════════════════╗
║                          BEFORE YOU DEPLOY                                 ║
╚════════════════════════════════════════════════════════════════════════════╝

📝 Checklist:

□ MongoDB Atlas account created
  → https://www.mongodb.com/cloud/atlas
  → Create free cluster
  → Get connection string
  → Whitelist IP (0.0.0.0/0 for any IP)

□ Generate JWT Secret (32+ characters)
  → python3 -c "import secrets; print(secrets.token_urlsafe(32))"
  → OR: openssl rand -base64 32

□ Domain name (optional but recommended)
  → Buy from Namecheap, GoDaddy, etc.
  → Point to your server IP

□ SSL Certificate (free with Let's Encrypt)
  → sudo certbot --nginx -d yourdomain.com


╔════════════════════════════════════════════════════════════════════════════╗
║                          AFTER DEPLOYMENT                                  ║
╚════════════════════════════════════════════════════════════════════════════╝

✅ Testing:

1. Health Check:
   curl http://your-server-ip:8000/health

2. Register a user:
   • Visit http://your-server-ip
   • Click "Sign Up Free"
   • Create account

3. Upload a CV:
   • Login
   • Go to Upload
   • Drag & drop a PDF

4. View Analysis:
   • See your analysis results
   • Check History page


╔════════════════════════════════════════════════════════════════════════════╗
║                         MAINTENANCE COMMANDS                               ║
╚════════════════════════════════════════════════════════════════════════════╝

View logs:
   docker-compose logs -f

Restart services:
   docker-compose restart

Update app:
   git pull
   docker-compose up --build -d

Stop services:
   docker-compose down

Check status:
   docker-compose ps


╔════════════════════════════════════════════════════════════════════════════╗
║                          COMMON ISSUES                                     ║
╚════════════════════════════════════════════════════════════════════════════╝

❌ "MongoDB connection failed"
   → Check MONGODB_URI in .env
   → Whitelist IP in MongoDB Atlas
   → See: MONGODB_CONNECTION_FIX.md

❌ "Registration fails with 500 error"
   → Check MongoDB connection
   → Verify JWT_SECRET is set
   → See: REGISTRATION_TROUBLESHOOTING.md

❌ "Frontend shows blank page"
   → Check backend is running: curl http://localhost:8000/health
   → Check VITE_API_URL in frontend .env
   → Check browser console (F12)

❌ "CORS errors"
   → Add your domain to CORS_ORIGINS in backend .env
   → Rebuild: docker-compose up --build -d


╔════════════════════════════════════════════════════════════════════════════╗
║                          RECOMMENDED SETUP                                 ║
╚════════════════════════════════════════════════════════════════════════════╝

For production, we recommend:

🖥️  Server: DigitalOcean Droplet (2GB RAM, $12/month)
🗄️  Database: MongoDB Atlas (Free tier or $9/month)
🔒 SSL: Let's Encrypt (Free)
🌐 Domain: Namecheap/GoDaddy ($10-15/year)

Total monthly cost: ~$12-21/month

This gives you:
✅ Reliable hosting
✅ Automatic backups (MongoDB Atlas)
✅ HTTPS/SSL included
✅ Professional domain
✅ Easy to manage


╔════════════════════════════════════════════════════════════════════════════╗
║                          NEED MORE HELP?                                   ║
╚════════════════════════════════════════════════════════════════════════════╝

📖 Comprehensive Guide:
   → DEPLOYMENT_GUIDE.md (full details, all platforms)

📚 Other Documentation:
   → QUICKSTART.md
   → MONGODB_SETUP.md
   → PRODUCTION_CHECKLIST.md
   → START_HERE.txt

🐛 Troubleshooting:
   → REGISTRATION_TROUBLESHOOTING.md
   → MONGODB_CONNECTION_FIX.md
   → NO_LOGIN_ERRORS_FIX.md

💬 Community:
   → GitHub Issues
   → Discord (if available)


╔════════════════════════════════════════════════════════════════════════════╗
║                              SUCCESS!                                      ║
║                                                                            ║
║            Your PersonaLens app is now deployed! 🎉                        ║
║                                                                            ║
║                    Share it with the world! 🚀                             ║
╚════════════════════════════════════════════════════════════════════════════╝
